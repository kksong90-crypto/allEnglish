# 녹음체크 PWA v8.2.2 속도 후보

작성일: 2026-08-04 KST
브랜치: `agent/recording-speed-v2-20260804`

## 기준

`origin/agent/vnext-score-report-absence-ux`의 v8.2.0 방학·예정결석 이중 보호 UI를 기준으로 했다. GitHub `main`의 구형 단순 화면을 기준으로 하지 않는다.

## 변경

- 마지막으로 서버에서 확정한 화면을 12시간 이내에 한해 즉시 표시하고 백그라운드에서 최신 상태를 확인한다.
- 최신 확인 실패 시 저장 화면을 지우지 않고 오류 배너로 명확히 표시한다.
- 여러 학생을 빠르게 누르면 한 명씩 순서대로 전송해 Apps Script 잠금 경쟁을 피한다.
- 저장 성공 응답 전에는 제출 상태를 확정하지 않는 원칙을 유지한다.
- 일반 재연결과 마감 전 제출 인정 후 조회는 서버 공유 캐시를 강제로 비우지 않는다.
- 사용자가 누르는 `서버 상태 다시 읽기`만 강제 최신 조회를 유지한다.
- 빠른운영의 예정결석 안내를 녹음 확인·미제출 자동 면제 규칙과 일치시켰다.
- 빠른운영에서 예정결석을 취소할 때 `recordVersion`을 함께 보내 동시 수정 충돌을 검사한다.

## 검증

- 기존 녹음·예정결석 웹 계약: 18/18 통과
- 속도 계약: 7/7 통과
- 학생 화면 계약: 20/20 통과
- 빠른운영 예정결석 계약: 4/4 통과
- JavaScript 문법 검사 통과

총 49개 계약 검사를 통과했다. 이 브랜치의 GitHub 보존만 수행하며 Pages 반영·Apps Script 배포·CoreDB 조작은 수행하지 않는다.
