# 학생웹 6.6.0 · VOCA Station 연결 후보

기준 화면은 학생웹 6.5.2 자동 동기화·역할 안전 브랜치입니다. 기존 로그인, 학습일정, 성적 리포트, 예정결석·녹음 면제, `FORCED_CLOSED` 재시험 종료 처리를 유지한 채 단어 화면만 결합했습니다.

## 학생 화면

- `내 학습 단어`: 운영센터가 고정 `vocabBookId`와 Day 범위로 발행한 공식 배정
- `시험범위`: `plan_type=SCHOOL_EXAM`과 고정 `vocabBookId + revisionId + sectionIds`가 있는 학교시험·모의고사 범위
- `전체 단어장`: VOCA Station에서 `학생 공개`로 지정한 단어장 전체
- 중요단어: `VOCA:<안정 단어 ID>`로 기존 폴더와 동기화
- 포인트: 토요일~금요일, 50개 중 무작위 30문제, 실제 시험 8분, 최초 통과 시 주 1점

다른 공개 단어장을 열거나 연습해도 공식 배정, 다음 수업 범위, 포인트 범위는 바뀌지 않습니다. 일반 연습과 포인트 연습은 시간·횟수 제한이 없으며 결과·답안·오답을 저장하지 않습니다.

## 안전·속도 원칙

- 첫 진입에는 공개 단어장과 구간 목록만 받고, 단어는 Day·구간을 눌렀을 때만 가져옵니다.
- 검색은 학생 API를 거쳐 공개 Station 자료만 서버에서 최대 100개 가져옵니다.
- 표시 이름 유사도로 단어장을 연결하지 않습니다.
- `SCHOOL_EXAM` 고정 ID가 없는 기존 시험범위 문구는 ‘연결 대기’로 표시합니다.
- 연결된 단어장의 개정 또는 구간 ID가 바뀌면 범위를 추측하지 않고 ‘연결 갱신 필요’로 차단합니다.
- 학생 화면에는 Station 관리자 기능, API 주소, HMAC 속성명과 비밀값을 노출하지 않습니다.

## 현재 상태

로컬 구현과 계약 테스트만 완료했습니다. GitHub 푸시, 학생웹 배포, Student API 배포, Script Properties 설정, CoreDB 쓰기는 수행하지 않았습니다.
